# API Configuration
# ==================
# Development (local): Uses localhost backend
# Production (Netlify): Uses /api proxy (set in netlify.toml)

# For local development
VITE_API_BASE_URL=http://localhost:5000/api

# For Netlify production, set this in Netlify Dashboard:
# VITE_API_BASE_URL=/api
